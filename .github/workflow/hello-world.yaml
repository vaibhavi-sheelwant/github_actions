name: hello world
on:
 push:
  branches:
   -main 
jobs: 
first-hello-world-job:
runs-on:ubuntu-latest
steps:
 - name: chevkout code
   uses:actions/checkout@v3
 - name: print message
   run: echo" hello world"
